# frozen_string_literal: true

source "https://rubygems.org"

ruby "~> 4.0"

# The converter script uses only Ruby stdlib and ffmpeg (external binary),
# so there are no runtime gem dependencies. Add development/test gems below
# as needed.

group :development do
  gem "minitest"
  gem "rake"
  gem "rubocop"
end
